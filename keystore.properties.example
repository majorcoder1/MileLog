# Copy to keystore.properties and fill in. Never commit the real one.
# The release build is unsigned without it; debug builds do not need it.
storeFile=/absolute/path/to/your-release.jks
storePassword=
keyAlias=
keyPassword=
