# Build output
build/
.gradle/
*.apk
*.aab

# Local machine config
local.properties
.idea/
*.iml
.DS_Store

# Signing material — never commit these.
# Losing the key means you can no longer update an installed copy of the app.
keystore.properties
*.jks
*.keystore
